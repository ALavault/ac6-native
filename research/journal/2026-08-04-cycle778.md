# Cycle 778 checkpoint

- Lane: `bridge`; stock timing, read-only gameplay probe.
- `CModeTaskGame` exact vtable active through frame 11808.
- Mission update and `UpInput/UpObj/UpCam/UpRadio` progress.
- UnitManager: 230 objects; one exact `CAce6UnitPlayer`.
- Raw pitch/roll/button/trigger reach XAM with null controls.
- Zero calls to the older flight-model input/force hooks.
- Canonical factory size is 256 bytes; allocator poison at `player+10672`
  rejects that owner join rather than proving failed initialization.
- Decision: next join is player child-list `+216/+220` through update
  `0x822A6710`, not the central scheduler and not a renderer workaround.
