# Cycle 779

- Run `bridge` borné avec timing stock et profil neuf.
- Join runtime fermé : `CAce6UnitPlayer 0xB2470000` -> liste `+216/+220` ->
  enfant unique `0xB2470100`/table `0x82007A10` -> update `0x822A6710` ->
  transform joueur évolutif.
- G8 reste ouvert : les samples disponibles ne distinguent pas l'effet des
  axes du mouvement naturel.
- Ghidra Bridge utilisé sur le projet canonique ; export vtable absent et
  frontières tronquées, donc aucune régénération ni nom sémantique inventé.
- Prochain discriminant : commande enfant bornée autour des fronts XAM.
