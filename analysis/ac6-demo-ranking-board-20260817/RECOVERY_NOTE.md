# Exact recovery note

`ranking_ui_callback_contract.csv` is restored byte-for-byte from the locally
valid Ranking evidence archive. The remote ZIP had been truncated before this
member could be inflated.

The other members of this directory are imported from the cleartext recovery
already committed on `main`.
