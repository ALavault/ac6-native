# Regenerated evidence note

`mission_result_layout.csv` was cut in the truncated remote ZIP and no exact
local member survived. This copy was regenerated from the published report and
the cited PPC consumers. Its `provenance` column deliberately says
`regenerated_from_report_and_consumers`; it is not represented as a bit-identical
recovery.

The other members of this directory are imported from the cleartext recovery
already committed on `main`.
