# Ghidra export catalog

`functions/AAAA/0xADDRESS__SYMBOL.json` is the human-oriented view. Entries are relative symbolic links to the flat `../exports/` directory required by ghidra-ai-bridge. `metadata/` contains the global export tables and `manifest.json` is the searchable index.
