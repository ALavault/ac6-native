# Reconstruction source root

This directory is intentionally empty until a XEX-aware Ghidra import provides
verified addresses and code ranges. Future host-side code targets Windows and
Linux and must not embed proprietary game data.

