diff --git a/thirdparty/rexglue-sdk/include/rex/graphics/graphics_system.h b/thirdparty/rexglue-sdk/include/rex/graphics/graphics_system.h
index d030f7f9..a0dc55d1 100644
--- a/thirdparty/rexglue-sdk/include/rex/graphics/graphics_system.h
+++ b/thirdparty/rexglue-sdk/include/rex/graphics/graphics_system.h
@@ -42,6 +42,27 @@ namespace rex::ui {
 class WindowedAppContext;
 }  // namespace rex::ui
 
+// Frame-loop telemetry counters. Defined in graphics_system.cpp; see the comment
+// there for why the measurement lives in the tree instead of in a local patch.
+// Emission is gated on the frame_loop_telemetry_interval cvar (0 = off), so the
+// counters are always maintained but silent by default.
+namespace rex::graphics::frame_loop_telemetry {
+// GPU interrupt delivered to the guest, split by source (0 = vblank, 1 = EOP).
+void CountInterrupt(uint32_t source);
+// GPU interrupt dropped because the guest has not registered a handler yet.
+void CountInterruptWithoutHandler();
+// Guest asked for a frame to be presented (VdSwap).
+void CountGuestSwapRequest();
+// Host reached the swap-present stage for a guest frame. This is the stage
+// cycle 316 paired against VdSwap, so guest_swap_requests vs host_swap_presents
+// is the comparable ratio.
+void CountHostSwapPresent();
+// Host notified the present pacer that a frame was delivered. A later stage than
+// CountHostSwapPresent, and only reached while pacing is armed, so a zero here
+// with a non-zero host_swap_presents is normal, not a fault.
+void CountHostPresentDelivery();
+}  // namespace rex::graphics::frame_loop_telemetry
+
 namespace rex::graphics {
 
 class CommandProcessor;
diff --git a/thirdparty/rexglue-sdk/src/graphics/graphics_system.cpp b/thirdparty/rexglue-sdk/src/graphics/graphics_system.cpp
index e1916e69..ebac5f88 100644
--- a/thirdparty/rexglue-sdk/src/graphics/graphics_system.cpp
+++ b/thirdparty/rexglue-sdk/src/graphics/graphics_system.cpp
@@ -45,6 +45,77 @@ REXCVAR_DEFINE_STRING(swap_post_effect, "none", "GPU", "Swap post effect: none,
     .allowed({"none", "fxaa", "fxaa_extreme"})
     .lifecycle(rex::cvar::Lifecycle::kRequiresRestart);
 
+REXCVAR_DEFINE_INT32(frame_loop_telemetry_interval, 0, "GPU",
+                     "Emit a frame-loop telemetry line every N guest vblank interrupts (0 = off). "
+                     "Pairs guest swap requests with host present deliveries and splits GPU "
+                     "interrupts by source, so a runtime that runs without drawing can be "
+                     "characterised without a debugger.")
+    .range(0, 100000);
+
+// Frame-loop telemetry.
+//
+// A guest that runs but never draws can only be diagnosed by pairing what it
+// asks for against what the host delivers. Cycles 316 and 317 established that
+// pairing - 2 VdSwap calls against 2 host presents, 4250 source-0 interrupts
+// against 4 source-1 - but they did it with local instrumentation patches that
+// were never landed. Cycle 323 found those patches still sitting unapplied in
+// workspaces/ace-combat-6/patches/, which means a bare run reports zero for
+// every one of those numbers and a future cycle can read that as a regression.
+//
+// These counters live in the tree so the measurement survives regeneration.
+// They are unconditional increments of relaxed atomics on paths that already do
+// far more work; only the emission is gated, on frame_loop_telemetry_interval.
+namespace rex::graphics::frame_loop_telemetry {
+namespace {
+std::atomic<uint64_t> g_interrupts_by_source[2]{};
+std::atomic<uint64_t> g_interrupts_other_source{0};
+std::atomic<uint64_t> g_interrupts_without_handler{0};
+std::atomic<uint64_t> g_guest_swap_requests{0};
+std::atomic<uint64_t> g_host_swap_presents{0};
+std::atomic<uint64_t> g_host_present_deliveries{0};
+}  // namespace
+
+void CountInterrupt(uint32_t source) {
+  if (source < 2) {
+    g_interrupts_by_source[source].fetch_add(1, std::memory_order_relaxed);
+  } else {
+    g_interrupts_other_source.fetch_add(1, std::memory_order_relaxed);
+  }
+  if (source != 0) {
+    return;
+  }
+  const int32_t interval = REXCVAR_GET(frame_loop_telemetry_interval);
+  if (interval <= 0) {
+    return;
+  }
+  const uint64_t vblanks = g_interrupts_by_source[0].load(std::memory_order_relaxed);
+  if (vblanks % static_cast<uint64_t>(interval) != 0) {
+    return;
+  }
+  REXGPU_INFO(
+      "[frame-loop] vblank={} eop={} other_source={} no_handler={} guest_swap_requests={} "
+      "host_swap_presents={} pacing_notifications={}",
+      vblanks, g_interrupts_by_source[1].load(std::memory_order_relaxed),
+      g_interrupts_other_source.load(std::memory_order_relaxed),
+      g_interrupts_without_handler.load(std::memory_order_relaxed),
+      g_guest_swap_requests.load(std::memory_order_relaxed),
+      g_host_swap_presents.load(std::memory_order_relaxed),
+      g_host_present_deliveries.load(std::memory_order_relaxed));
+}
+
+void CountInterruptWithoutHandler() {
+  g_interrupts_without_handler.fetch_add(1, std::memory_order_relaxed);
+}
+
+void CountGuestSwapRequest() { g_guest_swap_requests.fetch_add(1, std::memory_order_relaxed); }
+
+void CountHostSwapPresent() { g_host_swap_presents.fetch_add(1, std::memory_order_relaxed); }
+
+void CountHostPresentDelivery() {
+  g_host_present_deliveries.fetch_add(1, std::memory_order_relaxed);
+}
+}  // namespace rex::graphics::frame_loop_telemetry
+
 namespace {
 constexpr bool kStoreShaders = true;
 
@@ -407,9 +478,12 @@ bool GraphicsSystem::GetLastSwapSubmission(system::GraphicsSwapSubmission* out_s
 
 void GraphicsSystem::DispatchInterruptCallback(uint32_t source, uint32_t cpu) {
   if (!interrupt_callback_) {
+    frame_loop_telemetry::CountInterruptWithoutHandler();
     return;
   }
 
+  frame_loop_telemetry::CountInterrupt(source);
+
   auto thread = system::XThread::GetCurrentThread();
   assert_not_null(thread);
 
@@ -448,6 +522,7 @@ void GraphicsSystem::SetGuestPresentPacing(double target_hz) {
 }
 
 void GraphicsSystem::NotifyGuestPresent() {
+  frame_loop_telemetry::CountHostPresentDelivery();
   g_guest_present_count.fetch_add(1, std::memory_order_relaxed);
   // Empty critical section closes the race with PaceGuestPresent's predicate
   // check-then-wait, so the notify below cannot fall between them and be lost.
diff --git a/thirdparty/rexglue-sdk/src/graphics/vulkan/command_processor.cpp b/thirdparty/rexglue-sdk/src/graphics/vulkan/command_processor.cpp
index 1004e847..c278ed7d 100644
--- a/thirdparty/rexglue-sdk/src/graphics/vulkan/command_processor.cpp
+++ b/thirdparty/rexglue-sdk/src/graphics/vulkan/command_processor.cpp
@@ -24,6 +24,7 @@
 #include <SPIRV/GlslangToSpv.h>
 #include <glslang/Public/ShaderLang.h>
 #include <rex/assert.h>
+#include <rex/graphics/graphics_system.h>
 #include <rex/cvar.h>
 #include <rex/dbg.h>
 #include <rex/logging.h>
@@ -2414,6 +2415,11 @@ void VulkanCommandProcessor::IssueSwap(uint32_t frontbuffer_ptr, uint32_t frontb
           frontbuffer_width_scaled, frontbuffer_height_scaled);
     }
   }
+  // Counted here, not at NotifyGuestPresent: this is the stage cycle 316 paired
+  // against VdSwap when it concluded the host honours exactly what the guest
+  // asks. Keeping the pairing at the same stage is what makes the two cycles
+  // comparable.
+  graphics::frame_loop_telemetry::CountHostSwapPresent();
   REXGPU_DEBUG(
       "XELOG_GPU PRESENT: swap_texture_view={:p} packet_size={}x{} src_size={}x{} "
       "src_unscaled={}x{} guest_output_size={}x{} format={}",
@@ -3638,6 +3644,11 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
 
   const RegisterFile& regs = *register_file_;
   (void)index_buffer_info;
+  ac6::backend::ReportHostIssueCalled(ac6::backend::BackendDrawApi::kVulkan);
+  auto draw_result = [](bool success) {
+    return ac6::backend::ReportBackendIssueResult(
+        ac6::backend::BackendDrawApi::kVulkan, success);
+  };
   auto draw_fail = [&](const char* stage) {
     auto vgt_draw_initiator = regs.Get<reg::VGT_DRAW_INITIATOR>();
     REXGPU_ERROR(
@@ -3655,7 +3666,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
   xenos::EdramMode edram_mode = regs.Get<reg::RB_MODECONTROL>().edram_mode;
   if (edram_mode == xenos::EdramMode::kCopy) {
     // Special copy handling.
-    return IssueCopy();
+    return draw_result(IssueCopy());
   }
 
   bool surface_pitch_is_zero = regs.Get<reg::RB_SURFACE_INFO>().surface_pitch == 0;
@@ -3688,7 +3699,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
   if (surface_pitch_is_zero && is_rasterization_done) {
     // Doesn't actually draw.
     // Unlikely that zero would even really be legal though.
-    return true;
+    return draw_result(true);
   }
   VulkanShader* pixel_shader = nullptr;
   if (is_rasterization_done) {
@@ -3708,7 +3719,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     // cache.
     if (!memexport_used_vertex) {
       // This draw has no effect.
-      return true;
+      return draw_result(true);
     }
   }
   bool memexport_used_pixel = pixel_shader && (pixel_shader->memexport_eM_written() != 0);
@@ -3755,7 +3766,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     }
     if (!primitive_processing_result.host_draw_vertex_count) {
       // Nothing to draw.
-      return true;
+      return draw_result(true);
     }
     if (primitive_processing_result.host_primitive_type == xenos::PrimitiveType::kTriangleFan) {
       // Vulkan uses the same fan-to-list conversion policy as D3D12.
@@ -3905,7 +3916,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
                                                 &pipeline_is_placeholder);
   if (REXCVAR_GET(async_shader_compilation) && pipeline_is_placeholder) {
     frame_used_async_placeholder_pipeline_ = true;
-    return true;
+    return draw_result(true);
   }
   if (pipeline == VK_NULL_HANDLE || pipeline_layout_provider == nullptr) {
     return draw_fail("pipeline_lookup");
@@ -4162,6 +4173,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
           PrimitiveProcessor::ProcessedIndexBufferType::kNone ||
       shader_32bit_index_dma) {
     deferred_command_buffer_.CmdVkDraw(primitive_processing_result.host_draw_vertex_count, 1, 0, 0);
+    ac6::backend::ReportHostDrawEmitted(ac6::backend::BackendDrawApi::kVulkan);
   } else {
     std::pair<VkBuffer, VkDeviceSize> index_buffer;
     switch (primitive_processing_result.index_buffer_type) {
@@ -4194,6 +4206,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
             : VK_INDEX_TYPE_UINT32);
     deferred_command_buffer_.CmdVkDrawIndexed(primitive_processing_result.host_draw_vertex_count, 1,
                                               0, 0, 0);
+    ac6::backend::ReportHostDrawEmitted(ac6::backend::BackendDrawApi::kVulkan);
   }
 
   // Invalidate textures in memexported memory and watch for changes.
@@ -4223,7 +4236,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     }
   }
 
-  return true;
+  return draw_result(true);
 }
 
 bool VulkanCommandProcessor::IssueDraw_MemexportReadbackFullPath(uint32_t total_size) {
diff --git a/thirdparty/rexglue-sdk/src/kernel/xboxkrnl/xboxkrnl_video.cpp b/thirdparty/rexglue-sdk/src/kernel/xboxkrnl/xboxkrnl_video.cpp
index f50ea25b..2b742da4 100644
--- a/thirdparty/rexglue-sdk/src/kernel/xboxkrnl/xboxkrnl_video.cpp
+++ b/thirdparty/rexglue-sdk/src/kernel/xboxkrnl/xboxkrnl_video.cpp
@@ -422,6 +422,10 @@ void VdSwap_entry(ppc_pvoid_t buffer_ptr,      // ptr into primary ringbuffer
                   ppc_pu32_t frontbuffer_ptr,  // ptr to frontbuffer address
                   ppc_pu32_t texture_format_ptr, ppc_pu32_t color_space_ptr, ppc_pu32_t width,
                   ppc_pu32_t height) {
+  // Counted before any validation: the question this answers is whether the
+  // guest asked for a frame at all, which a rejected request still answers.
+  rex::graphics::frame_loop_telemetry::CountGuestSwapRequest();
+
   // All of these parameters are REQUIRED.
   assert(buffer_ptr);
   assert(fetch_ptr);
